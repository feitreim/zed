# PR #49910: git_graph: Add button to open up the commit view

_Merged: 2026-02-23T17:38:44Z_
_PR: https://github.com/zed-industries/zed/pull/49910_


## Documentation Suggestions

### Summary
The code adds a "View Commit" button to the Git Graph UI that opens a detailed commit view for the selected commit. Git Graph itself is not currently documented, so this change warrants both introducing the Git Graph feature and documenting the new button functionality.

### Suggested Changes

#### 1. docs/src/git.md

- **Section**: New section after "File History" (around line 150)
- **Change**: Add
- **Target keyword**: git graph commit history
- **Frontmatter**: (No change needed - existing frontmatter is adequate)
- **Links**: At least 3 internal docs links:
  - Link to [Project Diff](#project-diff)
  - Link to [File History](#file-history) 
  - Link to [Command Palette](./getting-started.md#command-palette)
  - Marketing link: https://zed.dev/features (relevant for main version control feature)
- **Suggestion**:

```markdown
## Git Graph

> **Preview:** This feature is available in Zed Preview. It will be included in the next Stable release.

Git Graph displays your repository's commit history as a visual graph, showing branch relationships and commit connections. Each commit displays its hash, author, date, and message in a table format.

### Opening Git Graph

Open Git Graph from the Git Panel using the graph icon in the panel's header, or search for "git graph" in the [Command Palette](./getting-started.md#command-palette).

### Viewing Commit Details

Select any commit in the graph to view its changed files and diff statistics in the side panel. Click the "View Commit" button to open a full commit view that shows the complete diff for that commit. This view functions as a [multibuffer](./multibuffers.md), similar to [Project Diff](#project-diff) and [File History](#file-history).

You can also double-click any commit row to open the commit view directly.
```

- **Full-file brand pass**: Required: yes. The rest of git.md already follows brand voice conventions well, but there are a few minor improvements:
  - Line 9: Change "lots of Git features" to "Git features for version control" (more specific)
  - Line 17: "the state of your project at a glance" is slightly marketing-y; consider "the current state: active repository, branch, changed files, and staging status"
  - Line 127: "make it easier to spot exactly what changed" could be "show which words changed within modified lines"

- **Brand voice scorecard**:

  | Criterion            | Score | Notes |
  | -------------------- | ----- | ----- |
  | Technical Grounding  | 4/5   | Concrete features: visual graph, multibuffer functionality, specific UI elements |
  | Natural Syntax       | 4/5   | Natural flow, varied sentence structure, no obvious AI patterns |
  | Quiet Confidence     | 5/5   | States facts without hype; no superlatives |
  | Developer Respect    | 5/5   | Assumes technical competence; explains functionality clearly |
  | Information Priority | 5/5   | Purpose stated first, then access methods, then details |
  | Specificity          | 4/5   | Specific actions and UI elements; could mention keyboard shortcuts if available |
  | Voice Consistency    | 5/5   | Matches existing docs voice throughout |
  | Earned Claims        | 5/5   | All claims are verifiable and supported by implementation |
  | **TOTAL**            | 37/40 | Pass threshold met (all criteria 4+) |

### Notes for Reviewer

- Git Graph appears to be behind a feature flag (`GitGraphFeatureFlag`), confirming this is a Preview-only feature
- The `OpenCommitView` action exists but I didn't find a default keybinding for it in my search - may need to verify if one exists
- Consider whether to add Git Graph to the Action Reference table at the bottom of the git.md file once it has stable keybindings
