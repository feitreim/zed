# PR #49732: agent: Tweak subagent input params and docs

_Merged: 2026-02-20T16:49:30Z_
_PR: https://github.com/zed-industries/zed/pull/49732_


## Documentation Suggestions

### Summary
The subagent tool's API has been clarified: `prompt` renamed to `task` and `timeout` renamed to `timeout_secs`. The tool description has been significantly revised to provide clearer guidance on when to use subagents and emphasize that spawned agents lack access to the parent conversation's context. These changes improve the developer experience for agent tool usage.

### Suggested Changes

#### 1. docs/src/assistant/tools.md
- **Section**: Subagent tool section (if exists), or new section under tool descriptions
- **Change**: Update or Add
- **Target keyword**: using subagent tool in Zed
- **Frontmatter**:
  ```yaml
  ---
  title: Assistant Tools
  description: Learn how to use tools with Zed's AI assistant, including file operations, command execution, and spawning subagents for parallel tasks.
  ---
  ```
- **Links**: 
  - [Assistant configuration](/assistant/configuration)
  - [Agent mode](/assistant/agent-mode)
  - [Context in conversations](/assistant/context)
  - https://zed.dev/assistant (marketing link for main feature)
- **Suggestion**: 

> **Preview:** This feature is available in Zed Preview. It will be included in the next Stable release.

The subagent tool spawns an independent agent to handle a delegated task. Use it for:

- Running multiple tasks in parallel that would take significantly longer sequentially
- Completing self-contained tasks where you need the outcome but not intermediate steps
- Investigations where you need the conclusion but not the research trail

Avoid using subagents for simple operations you could accomplish with one or two direct tool calls—each agent has startup overhead.

**Context isolation**: Spawned agents cannot see your conversation history or attached context. Include all necessary information (file paths, requirements, constraints) in the task description.

**Parameters**:
- `label`: Short description shown in the UI (e.g., "Researching alternatives")
- `task`: Complete task description with all context the agent needs
- `timeout_secs`: Optional maximum runtime in seconds

When spawning multiple agents that write to the filesystem, provide guidance to avoid conflicts (e.g., assign different directories).

- **Full-file brand pass**: Required: yes. Review the entire tools.md file to ensure all tool descriptions meet the brand rubric (direct language, no hedging, technical grounding). Remove phrases like "simply" or "just" if present elsewhere in the file.

- **Brand voice scorecard**:

  | Criterion            | Score | Notes |
  | -------------------- | ----- | ----- |
  | Technical Grounding  | 5/5   | Specific technical details (context isolation, startup overhead) |
  | Natural Syntax       | 4/5   | Clear, direct sentences; slight room for tightening |
  | Quiet Confidence     | 5/5   | No hedging, clear guidance without overselling |
  | Developer Respect    | 5/5   | Treats reader as capable, provides actionable guidance |
  | Information Priority | 5/5   | Leads with use cases, then mechanics |
  | Specificity          | 5/5   | Concrete examples (file paths, directory conflicts) |
  | Voice Consistency    | 4/5   | Matches technical doc tone throughout |
  | Earned Claims        | 5/5   | No unsubstantiated claims |
  | **TOTAL**            | 38/40 |       |

  Pass threshold: all criteria 4+. ✓

### Notes for Reviewer
The API changes are minor (field renames for clarity), but the updated tool description contains significant improvements to usage guidance. The documentation should emphasize context isolation since this is a non-obvious behavior that affects how developers write task descriptions. If there's already agent/subagent documentation elsewhere, cross-link appropriately.
