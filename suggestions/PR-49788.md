# PR #49788: Improve ETW profiler action

_Merged: 2026-02-21T06:13:19Z_
_PR: https://github.com/zed-industries/zed/pull/49788_


```markdown
## Documentation Suggestions

### Summary
ETW profiling actions were refactored: `StopEtwTrace` renamed to `SaveEtwTrace`, and a new `RecordEtwTraceWithHeapTracing` action was added. The recording mode also changed from file-based to memory-based buffering.

### Suggested Changes

#### 1. docs/src/developing-zed.md (or docs/src/profiling.md if it exists)

- **Section**: Performance Profiling (or Windows Profiling)
- **Change**: Add/Update
- **Target keyword**: "Windows performance profiling Zed"
- **Frontmatter**:
  ```yaml
  ---
  title: Profile Zed's Performance on Windows
  description: Use ETW tracing to analyze CPU, GPU, disk, and memory usage in Zed on Windows.
  ---
  ```
- **Links**: Include links to:
  - `settings.md` or keybindings documentation
  - Any existing debugging/development docs
  - Link to `zed.dev` if there's a relevant marketing page about performance
  - External Windows Performance Analyzer documentation (microsoft.com)

- **Suggestion**:

> **Changed in Preview (v0.225).** See [release notes](/releases#0.225).

**ETW Tracing on Windows**

Zed supports Event Tracing for Windows (ETW) to capture detailed performance data. You can record CPU, GPU, disk I/O, and file I/O activity, with optional heap allocation tracking.

**Recording a trace**

Open the command palette and run:
- **`etw_tracing: Record Etw Trace`** — Records CPU, GPU, and I/O activity
- **`etw_tracing: Record Etw Trace With Heap Tracing`** — Includes heap allocation data for the Zed process

Zed prompts you to choose a save location for the `.etl` trace file.

**Saving or canceling**

While recording:
- **`etw_tracing: Save Etw Trace`** — Stops recording and saves the trace to disk
- **`etw_tracing: Cancel Etw Trace`** — Stops recording without saving

Zed buffers trace data in memory. Recordings automatically save after 60 seconds if you don't manually stop them.

**Analyzing traces**

Open `.etl` files with [Windows Performance Analyzer](https://learn.microsoft.com/en-us/windows-hardware/test/wpt/windows-performance-analyzer) to inspect CPU stacks, GPU usage, disk I/O patterns, and heap allocations.

**Note for existing keybindings**: The `etw_tracing::StopEtwTrace` action was renamed to `etw_tracing::SaveEtwTrace`. Update any custom keybindings.

- **Full-file brand pass**: Required: yes. If updating an existing page, review the entire file for hedging language ("simply", "just"), ensure second-person voice, and verify all keybinding references use `{#kb action::Name}` syntax.

- **Brand voice scorecard**:

  | Criterion            | Score | Notes |
  | -------------------- | ----- | ----- |
  | Technical Grounding  | 5     | Specific tools, file formats, technical details |
  | Natural Syntax       | 5     | Direct instructions, no awkward constructions |
  | Quiet Confidence     | 4     | Clear statements without hype |
  | Developer Respect    | 5     | Assumes competence, provides relevant details |
  | Information Priority | 5     | Leads with how-to, minimal fluff |
  | Specificity          | 5     | Named actions, file formats, time limits |
  | Voice Consistency    | 5     | Second person, present tense, direct |
  | Earned Claims        | 5     | No unsubstantiated claims |
  | **TOTAL**            | 39/40 | Pass |

### Notes for Reviewer

- I suggested creating a new section if Windows profiling docs don't exist yet. If there's already ETW documentation, update that section instead.
- The action rename (`StopEtwTrace` → `SaveEtwTrace`) is a breaking change for users with custom keybindings—worth highlighting in release notes.
- The Memory vs File mode change is internal but affects buffering behavior (60s timeout now means the trace saves automatically rather than growing unbounded on disk).
- Consider whether this belongs in developer-focused docs or user-facing troubleshooting docs—ETW tracing is primarily for diagnosing performance issues during development.
```
